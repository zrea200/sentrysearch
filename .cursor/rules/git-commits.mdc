---
description: 本仓库 Git 提交信息须中英双语；勿附加 Cursor 署名尾注
alwaysApply: true
---

# Git 提交约定

## 主题行（首行）

- 遵循 [Conventional Commits](https://www.conventionalcommits.org/) 的 `type` 前缀（`feat`、`fix`、`docs`、`chore`、`refactor`、`test` 等）。
- **同一行内中英双语**，格式：`type: 中文简述 | Short English summary`  
  示例：`docs: 补充 DashScope 计费说明 | Document DashScope API pricing`

## 正文（可选）

- 需要时可补充细节，中英分段或混写均可，以清晰为准。

## 署名

- **不要**在提交中手动追加 `Co-authored-by: Cursor <cursoragent@cursor.com>` 或类似 Cursor 自动署名。
- **`Co-authored-by: Cursor …` 的来源：** Cursor Agent / 集成在代表你执行 `git commit` 时，可按产品策略自动附加该尾注，**不是** Conventional Commits 或你手写 message 的一部分。
- **如何关闭（任选其一，以当前 Cursor 版本界面为准）：**
  - **IDE：** Cursor Settings → **Agent**（或 **Features → Agent**）→ **Attribution** → 关闭与 **Commit** / **co-author** 相关的选项（例如 *Commit attribution*）。
  - **CLI Agent：** 编辑 `~/.cursor/cli-config.json`，将 `attribution.attributeCommitsToAgent` 设为 `false`（官方字段名见 [Cursor CLI 配置](https://cursor.com/docs/cli/reference/configuration)）；若仍有尾注，再在设置里查 PR/Commit 的 Attribution 项。

若团队规范禁止第三方署名，请在各成员环境关闭上述选项，并在 `git log` 中抽查确认。
